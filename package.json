{
	"name": "@heiwa4126/clean-publish-scripts",
	"version": "0.0.1-rc.0",
	"description": "TypeScript hello world library with dual ES modules/CommonJS support. Features GitHub Actions trusted publishing to npmjs with Sigstore attestation.",
	"type": "module",
	"main": "./dist/core.cjs",
	"module": "./dist/core.js",
	"types": "./dist/core.d.ts",
	"exports": {
		".": {
			"types": "./dist/core.d.ts",
			"import": "./dist/core.js",
			"require": "./dist/core.cjs"
		}
	},
	"bin": {
		"clean-publish-scripts": "dist/main.js"
	},
	"files": [
		"dist/**/*.js",
		"dist/**/*.cjs",
		"dist/**/*.d.ts",
		"!dist/**/*.map"
	],
	"scripts": {
		"build": "tsup",
		"test": "vitest run",
		"test:run": "vitest run",
		"test:watch": "vitest",
		"clean": "rm -rf dist",
		"prepublishOnly": "pnpm run lint && pnpm run test && pnpm run clean && pnpm run build && pnpm run smoke-test",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"format:full": "pnpm pkg fix && biome format --write . && dprint fmt && textlint --fix \"**/*.md\"",
		"smoke-test": "node examples/ex1.mjs && node examples/ex2.cjs && tsx examples/ex3.ts && node dist/main.js",
		"prepack": "cp package.json package.json.bak && node scripts/clean-pkg.mjs",
		"postpack": "mv package.json.bak package.json"
	},
	"keywords": [
		"hello-world",
		"example",
		"demo",
		"typescript",
		"esm",
		"commonjs",
		"cli",
		"learning",
		"sigstore",
		"trusted-publishing",
		"github-actions"
	],
	"author": "heiwa4126 <heiwa4126@gmail.com>",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/heiwa4126/heiwa4126-clean-publish-scripts.git"
	},
	"license": "MIT",
	"devDependencies": {
		"@biomejs/biome": "^2.2.6",
		"@types/node": "^24.8.1",
		"tsup": "^8.5.0",
		"tsx": "^4.20.6",
		"typescript": "^5.9.3",
		"vitest": "^4.0.8"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"dependencies": {},
	"workspaces": [
		"."
	]
}
